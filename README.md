"# data processing tool collecting kernel logs via winlogbeat, filters them through logstach using a ruby filter. forwards them to a non-Sql MongoDB. Use the open-source rasa machine learning framework a an interface between the user and a database. possibility of overlaying telegram bot UI as front end." 

IT IS IMPORTANT THAT YOU READ RASA_GIT.PDF!!!! to understand what tools to install and how the project works.
